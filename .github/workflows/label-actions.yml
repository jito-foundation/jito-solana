name: "Issue Label Actions"

on:
  issues:
    types: [labeled, unlabeled]

permissions:
  contents: read
  issues: write

jobs:
  action:
    runs-on: ubuntu-24.04
    steps:
      - uses: dessant/label-actions@v4
