[package]
name = "solana-fee"
description = "Solana fee calculation"
documentation = "https://docs.rs/solana-fee"
version = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
edition = { workspace = true }

# -----------------------------------------------------------------------------
# Features
# -----------------------------------------------------------------------------

[features]
# WARNING: This feature should NOT be enabled by default in a production context.
# It enables unstable APIs which may change without notice, increasing maintenance risk.
# Rust convention suggests keeping unstable features opt-in.
agave-unstable-api = []

[dependencies]
# Dependencies required for core fee calculation logic.
agave-feature-set = { workspace = true }
solana-fee-structure = { workspace = true }
solana-svm-transaction = { workspace = true }
