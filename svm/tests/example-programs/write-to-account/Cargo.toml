[package]
name = "write-to-account"
version = "2.1.20"
edition = "2021"

[dependencies]
solana-program = { path = "../../../../sdk/program", version = "=2.1.20" }

[lib]
crate-type = ["cdylib", "rlib"]

[workspace]
